{
  "name": "the-bastion",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "bastion-core",
    "bastion-cli",
    "plugins/bastion-github"
  ],
  "scripts": {
    "build": "npm run build -w bastion-core && npm run build -w bastion-cli && npm run build -w plugins/bastion-github",
    "test": "npm run test -w bastion-core && npm run test -w bastion-cli && npm run test -w plugins/bastion-github"
  }
}
